{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Animals Server",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}/src",
      "cwd": "${workspaceFolder}/src"
    }
  ]
}
